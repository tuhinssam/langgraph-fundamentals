## How to get started

### Install poetry
`pip install poetry`

### Initialize poetry to install all required packages
go inside project folder and run:
`poetry init`

There are three folders inside src/ 
* multi_node_setup
* conditional_graph
* feedback_loop

Every folder has main.py, run `python main.py` to generate the graph

feel free to modify the code and run